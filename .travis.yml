language: java
script:
  - mvn clean verify --file ./simple-api/pom.xml
  #connect to docker hub
  - docker login -u $user -p $password

 #build
  - docker build -t $user/repo_simple_api:database
  - docker build -t $user/repo_simple_api:httpd
  - docker build -t $user/repo_simple_api:simple-api

  #images publication
  - docker push -t $user/repo_simple_api:database
  - docker push -t $user/repo_simple_api:httpd
  - docker push -t $user/repo_simple_api:simple-api
